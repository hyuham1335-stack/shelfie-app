{
  "project": "shelfie",
  "phase": "app-shell",
  "carried_reports": {
    "note": "이전 런의 미해소 보고를 어느 step이 받는지 명시한다. 배정하지 않으면 이월된다 — 네 건이 세 런을 그렇게 살아남았다 (ROADMAP 15).",
    "resolved_by_main_before_run": [
      "알라딘 호출 상한 160 → 260회 (docs/TRD.md)",
      "errorCodeSchema 500대 공백의 계약 쪽 (docs/API_SPEC.md — INTERNAL_ERROR)",
      "unidentifiedOverflowCount 승인 문구 (docs/UI_GUIDE.md)",
      "vitest.config.ts setupFiles — jest-dom 매처 · RTL 자동 cleanup"
    ],
    "assigned_to_steps": {
      "0": [
        "NEXT_PUBLIC_MAX_PHOTOS 를 env.ts 가 읽지 않음 (런 #2)",
        "2MB·4MB 이미지 상수 이중화 (런 #3)",
        "errorCodeSchema 에 INTERNAL_ERROR 추가 + 소비처 (런 #3)",
        "'50권까지만' 하드코딩 · unidentifiedOverflowCount 무표시 (런 #4)",
        "컴포넌트 테스트 9개의 중복 afterEach(cleanup) 제거"
      ],
      "1": [
        "photoCount 를 세션 상태가 보관 (런 #4)"
      ],
      "2": [
        "recommend_viewed 이중 계상 — 클라이언트는 보내지 않는다 (런 #4)"
      ],
      "5": [
        "ambiguous 후보에 proof 가 없음 — resolve 재호출로 승격 (런 #4)",
        "photoCount 를 BookList 에 전달 (런 #4)"
      ]
    },
    "not_assigned_this_run": [
      "recommend_failed 이벤트 부재 (런 #4) — PRD 7번 이벤트 표 신설이 선행이다. console.warn 을 유지하고 런 #6으로 넘긴다",
      "@anthropic-ai/sdk 0.68 타입이 TRD 7번 호출 규약보다 낡음 (런 #2·#3) — 이번 런은 SDK 를 건드리지 않는다",
      "무상태에서 relevant:false 2회 연속 무시 · mood_submitted.retry_index (런 #4) — 요청 스키마에 세션 진행 상태를 넣을 것인가는 계약 결정이라 API_SPEC 갱신이 선행이다. 런 #5는 클라이언트 책임으로 두고 서버 판정을 바꾸지 않는다"
    ]
  },
  "steps": [
    {
      "step": 0,
      "name": "contracts-debt",
      "status": "completed",
      "started_at": "2026-09-01T23:33:40+0900",
      "summary": "세 런째 이월된 계약 부채 정리: ① src/lib/env.ts 가 NEXT_PUBLIC_MAX_PHOTOS 를 정적 참조로 읽어 MAX_PHOTOS_CEILING(5, 비공개)으로 [1,5] 클램프 — MAX_PHOTOS 는 계속 env.ts export ② 이미지 전송 상한을 src/lib/env.ts 로 이관: MAX_OUTPUT_BYTES_PER_IMAGE(2MB)·MAX_OUTPUT_BYTES_TOTAL(4MB) — 이름 그대로 src/lib/image.ts 가 재수출하고, analyze 라우트는 @/lib/env 에서 직접 import(로컬 MAX_IMAGE_BYTES·MAX_REQUEST_IMAGE_BYTES 삭제) ③ errorCodeSchema 에 INTERNAL_ERROR(500) 추가 + analyze·resolve·recommend·events 의 '우리 응답이 우리 스키마를 어김' 분기를 502/400 → 500 INTERNAL_ERROR 로 이동(외부 장애 502 는 전부 유지), ERROR_MESSAGES 4곳·ErrorBanner MESSAGE 맵 채움 ④ BookList 가 MAX_IDENTIFIED_BOOKS 를 import 하고 unidentifiedOverflowCount Notice 추가 ⑤ 컴포넌트 테스트 9개의 파일 상단 afterEach(cleanup) 과 미사용 import 제거.",
      "completed_at": "2026-09-01T23:43:15+0900",
      "attempts": 1
    },
    {
      "step": 1,
      "name": "session-machine",
      "status": "completed",
      "started_at": "2026-09-01T23:43:15+0900",
      "summary": "src/lib/session.ts 신설 (순수 · fetch·React·저장소 0건). 상태 10종 판별 유니온 SessionState = SessionData & {status} (error 변형만 errorCode: ErrorCode 로 좁힘), SessionStatus, ShelfBook = {origin:\"photo\";book:IdentifiedBook} | {origin:\"resolved\";book:ResolvedCandidate} (승격분에 photoIndex 를 지어내지 않는다), InputMode. SessionData 필드: sessionId·photoCount·books·unidentified·overflowCount·unidentifiedOverflowCount·failedPhotoIndexes·mood·inputMode·questions·recommendations·shortfall·recommendCount·errorCode·requestId. createSessionState(sessionId): SessionState (UUID 는 밖에서 주입). sessionReducer(state, action): SessionState — 정의되지 않은 조합은 던지지 않고 같은 참조 반환. SessionAction 15종: ANALYZE_STARTED{photoCount} · ANALYZE_RETRIED{photoCount} · ANALYZE_SUCCEEDED{result: AnalyzeResponse} · ANALYZE_FAILED{code,requestId}(EMPTY_SHELF→emptyShelf, 그 외→error) · BOOK_RESOLVED{rawText,book: ResolvedCandidate} · MOOD_STEP_ENTERED · GUIDED_MODE_ENTERED · QUESTIONS_RECEIVED{questions}(빈 배열→moodInput 폴백) · GUIDED_SKIPPED · MOOD_SUBMITTED{mood} · ANSWERS_SUBMITTED{mood} · RECOMMEND_SUCCEEDED{recommendations,shortfall} · RECOMMEND_FAILED{code,requestId}(IRRELEVANT_MOOD·RECOMMENDATION_VALIDATION_FAILED→moodInput, 그 외→error) · RECOMMEND_AGAIN · RESTARTED. 셀렉터: toBookReferences(state): BookReference[] · toRecommendBooks(state): RecommendBook[](승격분 claudeNote=\"\") · canRecommendAgain(state): boolean · hasVerifiedBooks(state): boolean, 상수 MAX_RECOMMEND_ATTEMPTS=5. 이월 보고 \"photoCount 를 세션 상태가 보관\" 해소 — ANALYZE_STARTED/RETRIED 가 기록한다. 판단 두 가지를 명시한다: ① RESTARTED 는 recommendCount 를 초기화하지 않는다(FR-010 상한이 세션당이라 \"새 사진으로 시작\"으로 풀리면 상한이 없어진다) ② 책장이 50권일 때의 BOOK_RESOLVED 는 상태를 바꾸지 않는다(미확인 원문을 지우고 책을 버리면 조용한 소실이 된다). 테스트 55개 — 상태도 22개 전이 전부 + 미정의 (상태×액션) 조합 전수 무시 검증.",
      "completed_at": "2026-09-01T23:51:10+0900",
      "attempts": 1
    },
    {
      "step": 2,
      "name": "api-client",
      "status": "completed",
      "started_at": "2026-09-01T23:51:10+0900",
      "attempts": 1,
      "summary": "src/lib/api-client.ts 신설 — 클라이언트의 유일한 네트워크 경계 (import 는 @/lib/schemas 의 errorResponseSchema · @/types/api · @/types/book 뿐. 서버 전용 모듈 0건). 결과 타입: type ApiResult<T> = {ok:true;data:T} | {ok:false;code:ErrorCode;requestId:string|null;status:number} — 응답을 받지 못한 경우(네트워크 단절·클라이언트 타임아웃) status 는 0 이고, 화면 분기 기준은 언제나 code 다. 시그니처 5종: analyzePhotos(sessionId:string, images:string[]): Promise<ApiResult<AnalyzeResponse>> · resolveBook(sessionId:string, query:string): Promise<ApiResult<ResolveResponse>> · fetchMoodQuestions(sessionId:string, books:BookReference[]): Promise<ApiResult<MoodQuestionsResponse>> · requestRecommendations(input:RecommendRequest): Promise<ApiResult<RecommendResponse>> · sendClientEvent(event:ClientEventPayload): Promise<void>(실패를 삼키므로 반환값 없음). export const CLIENT_TIMEOUT_MS = {analyze:70000, resolve:15000, questions:35000, recommend:35000, events:5000} — 전부 서버 하드 상한보다 길다(API_SPEC 공통 규약). export type ClientEventPayload 는 recommend_accepted{position:1|2|3} 와 book_resolved{resolve_attempt:number;matched:boolean} 판별 유니온이라 이월 보고 \"recommend_viewed 이중 계상\"이 타입으로 막힌다 — 보내려 하면 TS2322 로 컴파일이 깨지는 것을 프로브로 확인했다(라우트 recommend/route.ts:253 이 정본). 실패 정규화: 본문이 errorResponseSchema 를 통과하면 그 code·requestId 를 그대로 쓰고, 파싱 실패·계약 위반 시 상태 코드로 유추한다(504→TIMEOUT, 그 외 5xx→INTERNAL_ERROR, 나머지→INVALID_REQUEST). 504 를 5xx 일괄 INTERNAL_ERROR 에서 뺀 것은 타임아웃을 우리 결함으로 오귀속하지 않기 위해서다. requestId 는 본문 우선 → X-Request-Id 헤더 → null(지어내지 않는다). 200 인데 본문이 JSON 이 아니면 INTERNAL_ERROR(우리 계약 위반이므로 502 가 아니다). fetch 가 던지면 타임아웃 abort 는 TIMEOUT, 그 외는 UPSTREAM_UNAVAILABLE(status 0). throw 0건 · 재시도 루프 0건(재시도는 상태 머신과 화면의 결정). 테스트 35개. 단계 5 에 넘기는 두 가지: ① resolveBook 은 스텝이 명시한 시그니처대로 author 를 노출하지 않는다 — resolveRequestSchema 는 optional author 를 허용하므로 저자 tie-break 가 필요해지면 인자 추가로 확장한다 ② 성공 본문은 JSON.parse 후 타입 단언만 하고 zod 재검증을 하지 않는다 — 5개 라우트가 모두 응답 직전 자기 스키마로 safeParse 하고 위반 시 500 을 내므로 검증 경계가 서버에 있다."
    },
    {
      "step": 3,
      "name": "ui-upload",
      "status": "pending"
    },
    {
      "step": 4,
      "name": "ui-mood-recommend",
      "status": "pending"
    },
    {
      "step": 5,
      "name": "page-shell",
      "status": "pending"
    }
  ],
  "created_at": "2026-09-01T23:33:40+0900"
}
